# Normalize line endings: store as LF in the repo, check out as-is.
* text=auto eol=lf

# Binary files — never normalize
*.png  binary
*.jar  binary
*.zip  binary
gradle/wrapper/gradle-wrapper.jar binary